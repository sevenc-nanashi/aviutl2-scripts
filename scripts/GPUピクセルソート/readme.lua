-- ========================================================================================================================
-- 最新版をダウンロード：https://aviutl2-scripts-download.sevenc7c.workers.dev/GPU%E3%83%94%E3%82%AF%E3%82%BB%E3%83%AB%E3%82%BD%E3%83%BC%E3%83%88.anm2
-- 説明書をブラウザで読む：https://github.com/sevenc-nanashi/aviutl2-scripts/blob/main/scripts/GPU%E3%83%94%E3%82%AF%E3%82%BB%E3%83%AB%E3%82%BD%E3%83%BC%E3%83%88/README.md
--
-- GPUを使用してピクセルソートを行うスクリプト。
--
-- ┌ ⚠️ Warning
-- │ ソート方向のサイズが2048ピクセル未満である必要があります。
-- └────────
--
-- [ PI ]
--   - `direction`：方向（0=横方向、1=縦方向、2=横方向（反転）、3=縦方向（反転））
--   - `threshold_min`：しきい値の最小値
--   - `threshold_max`：しきい値の最大値
--   - `visualize_sort_range`：ソート範囲を可視化するか
--   - `visualize_color`：ソート範囲可視化の色（`0xRRGGBB`）
--   - `visualize_fill_opacity`：ソート範囲可視化の塗りの不透明度
--   - `visualize_source_opacity`：ソート範囲可視化時の元画像の不透明度
--
-- --< 更新履歴 >----------------------------------------------------------------------------------------------------------
--
-- [ v1.0（2026/6/7） ]
--   - 初版リリース
--
-- ========================================================================================================================
