Default:
  Tips.ドット絵変形:
    effect.name: |
      ドット絵の拡大縮小・回転を行うスクリプト。

      標準描画と違い、これはドット絵でもドットのまま変形されます。
      また、発展補間オプションを有効にすると線がより綺麗に補間されます。
      （発展補間はまだ実験的機能です！バージョンの更新により動作が変わる可能性があります。）
    発展補間: |
      有効にすると、ドット絵の線が塗りの上に描画されるように補間され、線が消えにくくなります。
      また、拡大時に斜めの線がより綺麗に補間されます。
      この機能はcleanEdgeをベースにしています。cleanEdgeについてはこのページを参照してください： <https://torcado.com/cleanEdge/>
    基準色: |
      線の上書き判定に使う色。例えば#ffffffの場合は明るい色が優先されます。もしドット絵に外枠がある場合は、外枠を設定すると綺麗になります。
      cleanEdgeのHighest Colorに相当します。
    線の太さ: |
      線の太さを指定します。ピクセルが何マス分に広がるかを指定します。45度の線を綺麗にしたい場合は0.707付近にしてください。
      cleanEdgeのLine Widthに相当します。
    斜め補間: |
      拡大時に補間する傾斜を指定します。
      cleanEdgeのSlopesに相当します。

      - 1:1：45度の線のみ補間します。
      - 1:1 + 1:2：45度と26.565度（1:2の傾き）の線を補間します。
      - 1:1 + 1:2（補正）：45度と26.565度（1:2の傾き）の線を補間し、さらに1:2の線をより綺麗に補間します。
    補間閾値: |
      斜め補間をするときに、どのくらい似ている色を同じ色として扱うかを指定します。
      高めると似ている色の間が滑らかに補間されるようになりますが、高すぎると乱れが発生します。
      可能な限り低く設定することをお勧めします。
      cleanEdgeのSimilar Thresholdに相当します。
    ピクセル補正: |
      ドット絵を変形するとき、変形後の画像の位置がピクセルグリッドに乗らずにぼやけてしまうことがあります。
      （シーンの左上から見た画像の左上の位置が(1.3, 2.7)のように小数点になる場合）
      ピクセル補正を有効にすると、変形後の画像がピクセルグリッドに乗るように調整され、ぼやけを防止します。

      > [!TIP]
      > sigma-axis氏のaviutl2_script_PixelSnap_Sも同様の動作を行います： <https://github.com/sigma-axis/aviutl2_script_PixelSnap_S>

      # モード

      - 中心移動式：画像の左上がピクセルグリッドに乗るように中心点が調整されます。
      - 描画移動式：画像の左上がピクセルグリッドに乗るように描画位置が調整されます。
      - サンプラー式：AviUtl2のピクセル補間モードを変更します。
        このエフェクトの後にエフェクトを追加するとピクセル補正が無効になるかもしれません。
      - オフ：ピクセル補正を行いません。

    PI: |
      - `scale`：拡大率（1.0で等倍）
      - `scale_x`：X拡大率（1.0で等倍）
      - `scale_y`：Y拡大率（1.0で等倍）
      - `center_x`：中心X（ピクセル単位）
      - `center_y`：中心Y（ピクセル単位）
      - `angle_deg`：回転（度）
      - `enable_cleanedge`：発展補間
      - `highest_color`：基準色
      - `line_width`：線の太さ
      - `slopes`：斜め補間（0 = 「1:1のみ」、1 = 「1:1 + 1:2」、2 = 「1:1 + 1:2（補正）」）
      - `similar_threshold`：補間閾値
      - `alpha_grid`：透明グリッド
      - `pixelsnap`：ピクセル補正（1 = 中心移動式、2 = 描画移動式、3 = サンプラー式、0 = オフ）
      - `debug`：デバッグモード

English:
  Tips.ドット絵変形:
    effect.name: |
      Scales and rotates pixel art.

      Unlike the standard drawing behavior, this keeps pixel art looking pixelated while transforming it.
      When advanced interpolation is enabled, lines are interpolated more cleanly.
      (Advanced interpolation is still experimental. Its behavior may change in future versions.)
    発展補間: |
      When enabled, pixel art lines are interpolated so they are drawn over fills, making lines less likely to disappear.
      It also interpolates diagonal lines more cleanly when scaling up.
      This feature is based on cleanEdge. See this page for details about cleanEdge: <https://torcado.com/cleanEdge/>
    基準色: |
      The color used to determine line overwrite priority. For example, `#ffffff` prioritizes brighter colors. If the pixel art has an outline, setting the outline color can produce cleaner results.
      Corresponds to cleanEdge's Highest Color.
    線の太さ: |
      Specifies the line width. This controls how many cells a pixel expands across. Use a value around 0.707 to clean up 45-degree lines.
      Corresponds to cleanEdge's Line Width.
    斜め補間: |
      Specifies which slopes are interpolated when scaling up.
      Corresponds to cleanEdge's Slopes.

      - 1:1: Interpolates only 45-degree lines.
      - 1:1 + 1:2: Interpolates 45-degree and 26.565-degree (1:2 slope) lines.
      - 1:1 + 1:2 (Cleanup): Interpolates 45-degree and 26.565-degree (1:2 slope) lines, and further cleans up 1:2 lines.
    補間閾値: |
      Specifies how similar colors must be to be treated as the same color during slope interpolation.
      Higher values make transitions between similar colors smoother, but too high a value can cause artifacts.
      Set this as low as possible.
      Corresponds to cleanEdge's Similar Threshold.
    ピクセル補正: |
      When pixel art is transformed, the transformed image can become blurry if its position does not align to the pixel grid.
      (For example, when the top-left position of the image from the top-left of the scene is a decimal value such as (1.3, 2.7))
      Enabling pixel snap adjusts the transformed image so it aligns to the pixel grid and prevents blur.

      > [!TIP]
      > sigma-axis's aviutl2_script_PixelSnap_S performs similar behavior: <https://github.com/sigma-axis/aviutl2_script_PixelSnap_S>

      # Modes

      - Move Center: Adjusts the center point so the top-left of the image aligns to the pixel grid.
      - Move Drawing: Adjusts the drawing position so the top-left of the image aligns to the pixel grid.
      - Sampler: Changes AviUtl2's pixel interpolation mode.
        Adding another effect after this effect may disable pixel snap.
      - Off: Does not apply pixel snap.
    PI: |
      - `scale`: Scale factor (1.0 = original size)
      - `scale_x`: X scale factor (1.0 = original size)
      - `scale_y`: Y scale factor (1.0 = original size)
      - `center_x`: Center X in pixels
      - `center_y`: Center Y in pixels
      - `angle_deg`: Rotation in degrees
      - `enable_cleanedge`: Advanced interpolation
      - `highest_color`: Highest color
      - `line_width`: Line width
      - `slopes`: Slope interpolation (0 = "1:1 only", 1 = "1:1 + 1:2", 2 = "1:1 + 1:2 (cleanup)")
      - `similar_threshold`: Similar threshold
      - `alpha_grid`: Alpha grid
      - `pixelsnap`: Pixel snap (1 = move center, 2 = move drawing, 3 = sampler, 0 = off)
      - `debug`: Debug mode
  ドット絵変形:
    ドット絵変形: Pixel Art Transform
    中心移動: Move Center
    中心X: Center X
    中心Y: Center Y
    拡大縮小: Scale
    拡大率: Scale
    X拡大率: X Scale
    Y拡大率: Y Scale
    回転: Rotation
    回転（度）: Rotation (Degrees)
    発展補間設定: Advanced Interpolation
    発展補間: Advanced Interpolation
    線の太さ: Line Width
    斜め補間: Slope Interpolation
    "1:1のみ": 1:1 Only
    "1:1 + 1:2": 1:1 + 1:2
    "1:1 + 1:2（補正）": 1:1 + 1:2 (Cleanup)
    基準色: Highest Color
    補間閾値: Similar Threshold
    高度な設定: Advanced Settings
    透明グリッド: Alpha Grid
    ピクセル補正: Pixel Snap
    中心移動式: Move Center
    描画移動式: Move Drawing
    サンプラー式: Sampler
    オフ: "Off"
    デバッグモード: Debug Mode
    PI: PI
